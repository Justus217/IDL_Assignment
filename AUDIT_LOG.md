# Audit Log

| File Name | Problem Manifestation | Root Cause of Failure | Structural Correction | Git Commit Hash |
| --- | --- | --- | --- | --- |
| train.py | When running train.py the Program crashes with a FileNotFoundError | The configuration json file is missing, it is used to describe the model and parameters we want to use for training | We added a config.json in the root directory and reverse engineered the Fields needed for it from train.py | d6f775c7a897343e08f680f15ebb830ae9128a17 | 
| data.py | Loading the data failed with a FileNotFoundError in the get_loaders() function | The function assumes a suffix in the data filename "..._data.py" but our data does have different naming | We removed the suffix and make sure we specify the correct filename in the config.json | 5bb2d5f5a6b8c9d9bb54cd6070e84622ad4d5225 |
| models.py | We get a RunTimeError when trying to train a VGG16. It is a mismatch between expected number of channels and actual number of channels | The cause of this is that in VGGBlock when creating the layers in the for loop, the current_in_channels variable is never updated | We fixed this by updating the current_in_channels variable at the end of the for loop: We set it to out_channels, because the number of out_channels in this layer need to be the number of in_channels in the next layer | 755ed832b02b00c59fcfe51b82e0f4f4ab1117bd |
| models.py | For VGG16 we get a runtime error with matrix shapes which cant be multiplied | The cause of this error is that we always apply a padding of 1 in the VGGBlock. This messes up our dimensions, because for a 1x1 convolution we dont want padding. | We added a check similar to set the padding to 0 whenever we set the kernel size to 1 | b02926e7bb992f33a94f4cf94b1f979bda7859ac |
| fit.py | The CrossEntropyLoss errored because it encountered a multi dimensional target tensor | When we load the labels in the train_one_epoch function they have shape (batchsize, 1), but the CrossEntropyLoss expects shape (batchsize,) | We squeeze the label tensor with .squeeze() to fit the requirements of the Loss function | 1851a53000d4108d404ff7f5acdd1fd29cadeaa6 |
| fit.py | Same error as above just in the evaluate function | When we load the labels in the evaluate function they have shape (batchsize, 1), but the CrossEntropyLoss expects shape (batchsize,) | We squeeze the label tensor with .squeeze() to fit the requirements of the Loss function | 1ef9c9ab4614cc6b62fc3fa07a3987ba35264cfd |
| models.py | We get a runtime error during AlexNet training, that two matrices cant be multiplied | In the AlexNet class, the classifier has a wrong input dimension of 2048, it should be 3072. | We adjusted the input dimension of the classifier | e9a1ff3dee2962198361659c18735742b8a7923c |
| models.py | We get a type error when trying to train ResNet18, because we use a NoneType, where a Tensor is expected | In the forward function of ResNet18, we return nothing. | We added the return statement to the last line of the forward function | 9f3b54f48ca9732db1ff41b78c7050f315056996 |
| models.py | We get a RuntimeError trying to train the chest dataset with AlexNet. It is a mismatch of dimensions in the input and output. | The cause is that we dont pass the in_channels and num_classes in the init function, so the input and output dimensions are hardcoded and break for specific datasets | We add the in_channels and num_classes as parameters to the init function and use them in the corresponding layers | 0f1fb46ac477ae1d5cfb59c0a3b24688a4dd8212 |
| data.py | While looking at the data loading code we saw that the validation set and training set had overlapping data | The cause of this is that while loading the data we do not correctly spit training and validation set. The validation set is part of the training set. | We fix this by correctly slicing the training set, so that the validation and training set share no samples at all. | d68c997eb82f2bf89b867eec4d90ae2595bfd9d8 |
| train.py | Training the models achieves only very low accuracies | The drop-rate for the models was hard-coded to 99% in our training setup | We added the drop-rate as a hyperparameter to our config so we can easily adjust it | ad4260fe94e0982d22fdd657b5bbabf8b1e15ed5 |
| models.py | ResNet18 was always using the identity function as activation function. | The activation_str was hardcoded in the beginning of models.py and not loaded from the kwargs | We updated the Model initialization in train.py to use ReLU and also added a line in models.py to load the activation_str from the kwargs | 88b749546c1aab638f166362c006527670652cfe |
| fit.py | We noticed, that our gradients were not being set to zero during our training loop | The zero_grad() function of the optimizer was not called | We added the zero_grad() function for every batch loop so that our gradients are calculated correctly | 9a6f5a967331a9e10d573ee7487ef56f8979e23d | 